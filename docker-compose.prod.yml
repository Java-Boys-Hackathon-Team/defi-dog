name: defi-dog

# Продовый запуск на сервере: поднимается только само приложение.
#
#   docker compose -f docker-compose.prod.yml up -d --build
#
# База берётся из common-infra - общих сервисов сервера, к которым подключены
# все проекты. Поэтому docker-compose.yml здесь намеренно не подключается:
# описанные в нём postgres и pgadmin нужны только для локальной разработки, где
# проект должен быть самодостаточным.
#
# Параметры подключения и токены лежат в .env.prod рядом с этим файлом,
# права 600, в репозиторий не коммитятся.

services:
  defi-dog:
    build: .
    container_name: defi-dog
    restart: always
    # Рабочий каталог совпадает с путём на хосте намеренно: приложение просит
    # Docker запустить анализатор и монтирует ему каталог с исходниками по пути,
    # который видит само. Демон живёт на хосте, поэтому путь должен существовать
    # и там - иначе анализатор получает пустой каталог.
    working_dir: /srv/defi-dog
    entrypoint: ["java", "-Dspring.config.additional-location=file:/srv/defi-dog/application-prod.properties", "-jar", "/app/defi-dog.jar"]
    volumes:
      - './src/main/resources/application-prod.properties:/srv/defi-dog/application-prod.properties'
      - '/srv/defi-dog/sources:/srv/defi-dog/sources'
      # Анализаторы (slither, mythril) запускаются отдельными контейнерами через
      # демон хоста. Сокет даёт этому контейнеру полный доступ к Docker хоста -
      # осознанный размен ради работы сканеров; отключается снятием тома и
      # переменной DOCKER_HOST.
      - '/var/run/docker.sock:/var/run/docker.sock'
    ports:
      # Порт слушается на хосте: reverse proxy ходит в приложение через него.
      - "8082:8080"
    env_file:
      - .env.prod
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      # За обратным прокси, который терминирует TLS.
      - SERVER_FORWARD_HEADERS_STRATEGY=framework
    networks: [common-infra]

networks:
  common-infra:
    # Сеть поднята проектом common-infra; здесь она только используется.
    external: true
